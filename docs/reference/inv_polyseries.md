# Function to invert the polyseries

Function to invert the polyseries

## Usage

``` r
inv_polyseries(mu_target, size, sigma)
```

## Arguments

- mu_target:

  Target mean

- size:

  Number of units in the sequence

- sigma:

  Parameter for the growth. sigma = 0 is the logarithmic growth
